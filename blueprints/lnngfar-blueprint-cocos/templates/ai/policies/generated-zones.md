# 生成目录与工具目录策略

以下目录通常由模板、工具或引擎生成，AI 默认只读：

- `tools/fixtures/**`
- `tests/tools/**`
- `tools/check-*.js`
- `.creator/**`
- `extensions/**`

当任务目标是“升级门禁脚本或测试工具”时，允许修改上述目录，但必须同步：

1. 变更目的
2. 兼容性说明
3. 新增或更新对应测试
