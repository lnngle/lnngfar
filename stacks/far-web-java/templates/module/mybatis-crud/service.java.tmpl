@Service @RequiredArgsConstructor
public class {{Entity}}ServiceImpl extends ServiceImpl<{{Entity}}Mapper,{{Entity}}> implements {{Entity}}Service {
  public List<{{Entity}}VO> list({{Entity}}Query q){return baseMapper.selectPage(q);}
  public {{Entity}}VO get(Long id){return toVO(baseMapper.selectById(id));}
  @Transactional public {{Entity}}VO create({{Entity}}DTO d){ {{Entity}} e=toEntity(d); baseMapper.insert(e); return toVO(e); }
  @Transactional public {{Entity}}VO update(Long id,{{Entity}}DTO d){ {{Entity}} e=baseMapper.selectById(id); BeanUtils.copyProperties(d,e); baseMapper.updateById(e); return toVO(e); }
  @Transactional public void delete(Long id){ baseMapper.deleteById(id); }
}
