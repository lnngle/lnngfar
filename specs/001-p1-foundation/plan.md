# Implementation Plan: lnngfar Foundation

**Branch**: `001-p1-foundation` | **Date**: 2026-05-10 | **Spec**: [spec.md](spec.md)

## Summary

建立 lnngfar 的 pnpm Monorepo 基础工程，包含 4 个核心 package（types/logger/config-loader/workspace-manager），配备 ESLint + Prettier + Vitest + GitHub CI 完整工具链。

## Technical Context

**Language/Version**: TypeScript 5.3+ (strict mode)  
**Primary Dependencies**: pnpm 11.x, Vitest 1.2, ESLint 8.x  
**Storage**: N/A (P1 无持久化存储)  
**Testing**: Vitest with workspace mode  
**Target Platform**: Node.js 20+ (CLI), cross-platform  
**Project Type**: library (Monorepo packages)  
**Performance Goals**: N/A (无性能敏感路径)  
**Constraints**: 依赖方向 `types ← logger ← config ← workspace` 单向  
**Scale/Scope**: 4 packages, 15 tests, <500 lines per package

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Gate | Status | Evidence |
|------|--------|----------|
| TypeScript strict mode | ✅ | `tsconfig.base.json: "strict": true` |
| ESLint no-explicit-any | ✅ | `.eslintrc.json: "@typescript-eslint/no-explicit-any": "error"` |
| Prettier formatted | ✅ | `.prettierrc` singleQuote, trailingComma |
| Package naming @lnngfar/* | ✅ | packages 命名一致 |
| Tests ≥ 80% | ✅ | 15 tests covering all public APIs |
| CI lint + test + build | ✅ | `.github/workflows/ci.yml` |

## Project Structure

### Documentation (this feature)

```text
specs/001-p1-foundation/
├── spec.md              # Feature specification (User Stories + FR + SC)
├── plan.md              # This file (implementation plan)
├── research.md          # Phase 0 research (pnpm, vitest, logger design)
├── data-model.md        # Phase 1 data model (entities + validation)
├── quickstart.md        # Phase 1 quickstart (setup + test commands)
├── contracts/           # Phase 1 API contracts
│   └── foundation-contracts.md
├── tasks.md             # Phase 2 tasks (generated by /speckit.tasks)
└── checklist.md         # Quality checklist (generated by /speckit.checklist)
```

### Source Code (repository root)

```text
lnngfar/
├── package.json              # Root: pnpm workspace root
├── pnpm-workspace.yaml       # Monorepo 定义
├── tsconfig.base.json        # Shared TS config
├── vitest.workspace.ts       # Shared test config
├── .eslintrc.json            # ESLint rules
├── .prettierrc               # Prettier rules
├── .gitignore
│
├── packages/
│   ├── types/                # @lnngfar/types
│   │   ├── package.json
│   │   └── src/index.ts      # LnngfarConfig, Workspace, Project, Logger
│   │
│   ├── logger/               # @lnngfar/logger
│   │   ├── package.json
│   │   ├── src/index.ts      # createLogger()
│   │   ├── src/__tests__/index.test.ts
│   │   └── vitest.config.ts
│   │
│   ├── config-loader/        # @lnngfar/config-loader
│   │   ├── package.json
│   │   ├── src/index.ts      # loadConfig(), validateConfig()
│   │   ├── src/__tests__/index.test.ts
│   │   └── vitest.config.ts
│   │
│   └── workspace-manager/    # @lnngfar/workspace-manager
│       ├── package.json
│       ├── src/index.ts      # loadWorkspace(), createProject()
│       ├── src/__tests__/index.test.ts
│       └── vitest.config.ts
│
├── .github/workflows/ci.yml  # CI pipeline
│
├── specs/                    # Spec documents
│   ├── constitution.md
│   └── 001-p1-foundation/
│
├── .lnngfar/                 # lnngfar runtime data (P0 created)
│   ├── ai/
│   └── runtime/
│
└── docs/                     # Design documents
```

## Complexity Tracking

No violations to justify.
